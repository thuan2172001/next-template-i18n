
import React, { useEffect, useState } from "react";
import style from "./flex-padding.module.scss";

export const FlexPadding = () => {
    return (
        <div className={`${style["serie-component"]}`}>
        </div>
    )
}